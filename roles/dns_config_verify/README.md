# dns_config_verify role

Invoke play to Use the correct DNS

## Playbook:
* ~/playbooks/v_dns_config.yml

## Role:
* ~/roles/dns_config_verify

## Usage
```
ansible-playbook -i inventory/hosts playbooks/v_dns_config.yml
```
